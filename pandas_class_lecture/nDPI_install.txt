
git clone https://github.com/ntop/nDPI
cd nDPI
./autogen.sh
./configure
make
sudo make install
sudo ldconfig
./configure --enable-nDPI
cd ..


# dependencies
sudo apt-get install build-essential git bison flex 
     libpcap-dev libtool libtool-bin autoconf pkg-config 
     automake autogen libjson-c-dev libnuma-dev libgcrypt20-dev libpcre2-dev




 ~# git clone https://github.com/pmacct/pmacct.git
 ~# cd pmacct
 ~#  ./autogen.sh
 ~# ./configure #check-out available configure knobs via ./configure --help
 ~#  make
 ~#  make install #with super-user permission